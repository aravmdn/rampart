{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set for the main Rampart desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
