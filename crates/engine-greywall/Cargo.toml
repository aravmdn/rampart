[package]
name = "engine-greywall"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Greywall adapter scaffold for Rampart"

[dependencies]
semver = { version = "1.0", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
policy-core = { path = "../policy-core" }
thiserror = "2"
